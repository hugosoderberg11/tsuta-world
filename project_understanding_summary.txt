# Project Understanding Summary

## Recent edits (2026-09-14)
- TOP SERVICE 03 AI×WEB photo: `assets/service-aiweb-photo.jpg` mask knocked out (surrounding black → white) for white page background; CSS unchanged.
- TOP Hero title overlap: 「採用」was under the photo parallelogram. Raised `.hero-copy` / `.hero-copy-main` / `.hero-title` z-index above `.hero-visual` so the heading paints on top.
- TOP SERVICE 01 photo: slightly larger — column 46%→52%, image scale 108%→116%; vertical slogan shifted to stay just outside the photo. Copy/layout otherwise unchanged.
- TOP SERVICE 05 bottom edge: jagged under-camera cut replaced with a straight diagonal; leftover black stub removed. Camera body kept.
- TOP SERVICE 04 animation photo: swapped to client profile crop (`assets/service-animation-photo.png`); black canvas knocked out (hair kept); CSS clip-path off, `object-fit: contain`.
- TOP SERVICE 02/03 photo edges: black/jagged JPEG mask cleaned to transparent PNG; CSS clip-path off; `object-fit: contain`.
- TOP SERVICE mobile whitespace (`index.html` CSS): 04–06 stayed content-width on phones because `.top-svc-trio` is a column flex with `align-items: flex-start` and `flex: 1 1 0`, so cards (~194px) sat on the left with a large empty gutter. ≤960 now stretches cards to full width, forces visual/clip `width: 100%`, overrides per-item aspect-ratios to 16/10, and stacks the `sc-for` wrapper as a column. Pair/trio gaps 56/48→32. Desktop 1200+ unchanged.
- TOP Hero float-drop (`index.html` CSS): below 1200px the photo is no longer `position: absolute` inside a `100svh` frame (that left a tall empty column and let the parallelogram overflow). Tablet 961–1199 and 768–960 now size the visual to the image (`height: auto`, `transform: none`, `max-width: 100%`). Mobile ≤767 keeps the copy→photo stack and drops the leftover 280px min-height / 1.06 scale. Desktop 1200+ unchanged.
- TOP ABOUT deco (`index.html` only): removed PEOPLE / TECHNOLOGY / CREATIVE / SOCIETY and the vertical 働くを、もっと好きに。 UPDATE THE WAY… and large ABOUT remain. Other sections unchanged.
- TOP Hero slogan (`index.html` only): 「伝わるから、世界は動く。」 is now horizontal (was `writing-mode: vertical-rl` at top-right) and sits where the `01` pager + line were (bottom-right of the photo). Pager markup and CSS removed. Tablet/mobile keep the slogan in that same corner; PEOPLE/BRANDS aside and other hero copy unchanged.
- TOP ABOUT responsive deco (`index.html` CSS only): fixed pale-blue "ABOUT" hidden on mobile — root cause was `.top-about-deco` stacking context (z-index 1) under photo (z-index 2). ≤960px stage is now a 2-col grid (photo | deco flex column); photo narrowed (58/54/50vw); deco text no longer absolute-overlapped. Desktop unchanged.
- TOP SERVICE responsive type (`index.html` CSS only): bumped pinned mobile/tablet sizes — catch 24/23/21px (mid/low), desc 16/15px, en 16/15px, VIEW MORE 14/13px, numbers 72/64px. Fixed 600px breakpoint shrinking text further than 960px. Desktop unchanged.
- TOP Hero photo only (`index.html` + `assets/hero-woman-new.jpg`): hero background swapped to the client-supplied parallelogram crop. `object-fit: contain` + centered so the full image shows; CSS `clip-path` on `.hero-photo` removed (shape is in the asset). Scaled up (`max-width/height 118%`, `scale(1.12)`) so the woman reads larger. Responsive fix: tablet/mobile switch `.hero-photo`/`img` from absolute to flow layout so the visual no longer collapses/float-drops; mobile drops fixed `aspect-ratio`, hides `.hero-cut`. Copy, deco, other sections unchanged.
- TOP SERVICE 4-point polish (`index.html` CSS only): (1) 01–06 English labels vertically centered with numbers via `align-items: center` on `.top-service-kicker`, uniform `gap: 0 12px`, removed per-item top padding. (2) 02–06 thumbnails get per-item `clip-path` diagonal cuts (bottom-left notch on 02/04/05, top-left slant on 03/06) instead of plain rectangles. (3) 01 catch copy enlarged (`0.018→0.0235 * --svc-w`, line-height 1.32; 30px/26px at breakpoints). (4) 01 hero photo column widened (`40%→46%`), image scaled to 108%. Vertical slogan position updated. Layout, copy, VIEW MORE, other sections unchanged.
- TOP MEDIA aside copy (`index.html` CSS): 「私たちの取り組みが / さまざまなメディアで紹介されています。」 was wrapping to ~5 lines because the right column was `minmax(150px, 0.5fr)`. Desktop grid last column is now `max-content` with `white-space: nowrap` (the existing `<br>` still makes exactly 2 lines). Tablet 961–1199 puts the aside on its own row so it stays 2 lines. Other MEDIA/card design unchanged.
- TOP responsive (`index.html` CSS only, Desktop 1200+ unchanged): client reported collapse when narrowing. Cause was HERO staying 2-column down to phone widths plus `.top-hero-shell { min-height: 100svh }` leaving a tall empty gap. Tablet 768–1199 keeps side-by-side HERO (viewport-tall, no extra white). Mobile ≤767 stacks HERO copy then photo, reflows CONTACT overlays into a readable block, scales NUMBERS 2-col, clips ABOUT deco overflow. MEDIA/SERVICE/NEWS/FOOTER keep their existing per-section tablet rules rather than a sitewide column stack. Verified 1440 / 1024 / 768 / 430 / 390 / 375: no page horizontal scroll.
- TOP NUMBERS count-up (`scroll-motion.js` only): the previous IntersectionObserver never ran on the visible DOM. `initNumbersCountUp` bound `#numbers` inside hidden `<x-dc>` (or returned before React mounted `#dc-root`), so the live stats stayed at 1.9 / 70 / 200 / 400. Now waits for the live `#numbers` (not in `x-dc`), starts once when it enters the viewport (0.0→1.9, 0→70, 0→200, 0→400, ~1.9s sine ease-out), rebinds if dc-runtime remounts, and `prefers-reduced-motion` shows finals immediately. NUMBERS design and other sections unchanged.
- TOP SERVICE 01 photo: the supplied mock is used as-is (`assets/service-01-full.jpg`) with no crop. Hero clip-path / `object-fit: cover` / CSS blue parallelogram / HTML “Fans Create the Future.” overlay are off so the full image (couple + script) shows. Vertical slogan kept. 02–06 and SERVICE layout otherwise unchanged.
- TOP SERVICE 01–06 numbers + English labels only (`index.html` CSS): no overlay images in the repo, so HTML type was scaled to the client mock. 01 ≈ `0.068 * --svc-w` (~95px at 1400), 02–03 / 04–06 slightly smaller. Labels ~14–17px, weight 600, line-height 1.18, tighter gap to the number, color `#3A5F8F`. Catch / body / VIEW MORE / photos / layout untouched. Other sections untouched.
- TOP Hero left copy (`index.html` only): shifted the kicker/title/lead/CTA/SCROLL cluster right (`padding-left` + copy-main inset). Title/lead/kicker/CTA scaled up. SCROLL is 16px with a longer 108px rail, thicker blue traveler, and a small down arrow nudge. Right visual and lower sections untouched.
- TOP Hero SCROLL: replaced the invisible 1px gray ::after with an explicit `.hero-rail-travel` span — 2px `#1E4ED8` bar, `translateY` top→bottom, 1.8s linear infinite, fade at the ends. SCROLL type unchanged. Base line still 1px `#111318`. `prefers-reduced-motion` hides the traveler.
- TOP Header/Hero type scale (`index.html` hero/header CSS only): nav 13→15, CONTACT 13→15, logo 36→40, header 84→92. Title/lead/kicker/slogan/aside/pager/CTA bumped ~12–15%. Lower sections untouched.
- Sitewide logo: official TSUTA-WORLD mark (cyan/gray globe-arrow + TSUTA-WORLD). Black background knocked out to transparency (`assets/tsuta-logo.png`, same file for the navy footer). Favicon is the cropped globe mark. Header height ~36px, footer ~34px.

## Recent edits (2026-09-11)
- TOP FOOTER (`index.html` only): denser 4-col navy bar matching the client mock. Inner `min(90vw,1400px)`, padding ~52/48, compact logo + taglines, 13px headings / 13px links, thin `rgba(255,255,255,.18)` rules after SERVICE and COMPANY, white SNS icons, legal + © 2026 in FOLLOW US. Existing URLs kept; unset hrefs unchanged. Tablet 2-col / mobile stack, rules off. Other sections unchanged.
- TOP CONTACT (`index.html` only): finished to the client thin-banner mock. High-res crop is 1280×320 (`contact-banner.jpg`, q95) shown `width:100%; height:auto`. Overlays match mock: handwritten left, LET'S MAKE IT HAPPEN. + lead at ~47%, pill CTA mid-right, small UPDATE stack far right. Other sections unchanged.
- TOP NEWS (`index.html` only): matched client list mock. NEWS is Space Grotesk 500 (~56px), lead is お知らせ (regular 16px), VIEW ALL sits closer underneath. Inner `min(90vw,1400px)`; 2px gray rail; date `YYYY.MM.DD` + bold 16px titles with tight 14px row padding and no heavy rules; larger blue ← →. Existing article URLs kept. Other sections unchanged.
- TOP ABOUT US (`index.html` only): large deco ABOUT nudged right (`.top-about-deco-big` `left: 80% → 88%` of photo width) so the leading A is no longer covered by the building clip. Size/color/font and other ABOUT elements unchanged. Other sections unchanged.
- TOP NUMBERS (`index.html` + `scroll-motion.js` only): added IntersectionObserver count-up for 1.9億+ / 70社+ / 200万人+ / 400+ (1.75s ease-out, once on first view; `prefers-reduced-motion` shows final values immediately). Numeric spans only; 億/社/万人/+ suffixes unchanged. Tightened NUMBERS→ABOUT vertical gap by reducing `.top-numbers-section` bottom padding and `.top-about-section` top padding at all breakpoints (no double-stacking). NUMBERS layout/type/colors untouched. Other sections unchanged.

## Recent edits (2026-09-10)
- TOP SERVICE final micro pass (`index.html` inline CSS only): locked 04–06 onto one row via explicit `grid-column`/`grid-row` on `.top-svc-trio > .top-svc-low`, `repeat(3, minmax(0,1fr))` on the trio wrapper, and a pre-hydration fallback grid on `#services .top-svc-trio > sc-for`. Row gaps tightened slightly (`margin-top: 14→10` on pair/trio). 01–03 layout/type/image sizes untouched. Nothing outside `#services` touched.
- TOP SERVICE 2-point fix (`index.html` CSS only): 04–06 row switched to `display:flex` (nowrap, equal `flex:1`) so all three stay on one line; trio cards slightly tighter internal grid (`34%/56%`). 01 vertical copy 働くを、もっと好きに。 restored with `position:absolute` just outside the hero photo right edge (`writing-mode: vertical-rl`). 01–03, script, heading unchanged. Nothing outside `#services` touched.
- TOP SERVICE compact pass (`index.html` inline CSS only, HTML unchanged): tightened density to match the ideal mock without rebuilding the section. Section padding `88/66→76/52`, head margin `42→28`, row gaps `21–22→14` (~33% tighter). 01 photo column `47%→40%`, copy/photo gap `1.9%→1.2%`, aspect `1.7→1.85` (shorter), script smaller and repositioned (`left -15%→-9%`), vertical 働くを… pulled toward the photo via `translateX`. 02–03 mid tier typography scaled down (~12%) with lighter catch weight (600); 03 offset removed. 04–06 trio uses `repeat(3, minmax(0,1fr))` so all three stay on one row; per-card `margin-top` offsets on 05/06 removed and internal grids unified (`36%/58%`). Three-tier size hierarchy: hero idx/name largest, mid mid-sized, low smallest. English labels smaller and closer to numbers. Nothing outside `#services` touched.
- TOP SERVICE layout rebuilt to the 9/5 mock as a spec (measured from the mock image, not eyeballed). `.top-service-inner` now sets `--svc-w: min(90vw, max(1400px, 73vw))` and every SERVICE size is `calc(var(--svc-w) * k)`, so the whole block keeps the mock's proportions at any width (1400px is the reference; 1920px desktop is the primary target). Geometry from the mock: 01 = copy 33.5% / photo 56% (aspect 1.86) / vertical slogan in the remaining right margin; 02–03 = two 46% blocks (photo 47%, copy 47%) with only `margin-top: 28px` above the row; 04–06 = `33% / 33.5% / 32.5%` blocks with `margin-top: 29px`, 04 photo wide (54%, aspect 1.36) and 05/06 near-square (38%, aspect ~0.96). Total 01→06 content height ≈ 1080px, matching the mock (was ≈ 1660px). Type at 1400px: numbers 80/70, English labels 13.5, catch 33/27/22, body 17/15, VIEW MORE 14. Number blue re-sampled from the mock core pixels: `#33598C`. All catch/desc line breaks render exactly as the mock (01 = 3 lines, others = 2). Photos tilt down-right except 01, which tilts up-right. Below 960px the fluid sizes are pinned to fixed px in the media queries. Nothing outside `#services` was touched.
- TOP SERVICE first pass (`index.html` + `scroll-motion.js` head clip): matched 9/5 21:30 mock. SERVICE title uses MEDIA type (Space Grotesk 700, `letter-spacing: 0.02em`, not Cormorant italic); size kept large (`clamp(56px, 7.2vw, 92px)`). Banners 01–06 replaced with provided photos (`assets/service-*-photo.jpg`), parallelogram clips, `object-fit: cover` (not graphic 16:9 contain). Numbers use mock steel blue `#3A5F8F`, Barlow Condensed 600, smaller scale. English labels wrap beside numbers as one heading group. Catch/desc line breaks follow the mock; 03 price hidden. GSAP clipPath on `.top-service-head` removed (was cutting SERVICE). Other sections unchanged.
- TOP MEDIA aside copy: 2 lines (私たちの取り組みが / さまざまなメディアで紹介されています。). GSAP `initMedia` clipPath on `.top-media-head` removed (was clipping “A” in MEDIA). Head uses fade-up only; cards slide from right not left. CSS: wider head column (min 280px), `margin-left: -20px`, z-index on head. `#media sc-for { display: contents; }` so two cards sit in one row (was stacking inside sc-for wrapper). Inner container matches SERVICE (`min(1400px, calc(100% - 48px))`); removed narrow `top-section`/`top-wide-section` wrapper. Grid gives more width to card column (0.5/2/0.58fr); card gap 32px. Section padding `120px 0 40px` (was 160/180) to tighten gap before SERVICE. Thumbnails scale up via wider container; 16:9 kept. Other sections unchanged.
- TOP Hero (index.html + scroll-motion.js only): layout matched to 9/5 reference mock. Grid is 38fr/62fr (not 50:50); copy column narrower, visual wider to edge. Title/lead sizes and photo clip-path/object-position tuned. Tablet–mobile keeps side-by-side grid (40/60 → 44/56) — no vertical stack. Subtle GSAP entrance: copy fade-up, visual delayed fade/slide, deco slow float; CSS fallback + prefers-reduced-motion. Other sections unchanged.

## Recent edits (2026-09-09)
- TOP SERVICE heading: Japanese lead sits immediately to the right of SERVICE (not far-right); Noto Serif JP 700.
- TOP header hamburger is 3 stacked bars (was one line from row flex) and sits at the far right of the header. SERVICE 01 “Fans Create the Future.” moved to the photo’s lower-left.
- TOP ABOUT headline is two lines: 伝わらないを、 / なくしていく。
- Sitewide FOOTER: HTML/CSS recreation of the dark navy 4-column mock (not an image). Background `#001722`, max-width 1400px, padding `55px 5vw 45px`, height ~350px on desktop. Columns: brand (logo + まだ見ぬ可能性を、世界へ。 + UPDATE THE WAY / THE WORLD CONNECTS.) / SERVICE / COMPANY / FOLLOW US, with 1px vertical rules after SERVICE and COMPANY. Existing service/company URLs kept; メンバー / sitemap / Instagram / TikTok / X use `data-href-unset` (no invented URLs). YouTube uses `@tsutaworld733`. Privacy → tsuta-world.com/privacy/, recruit → tsuta-world.com/recruit/. White knockout logo `assets/tsuta-icon-white.png` for the dark bar. Tablet 2-col, mobile stacks brand→SERVICE→COMPANY→FOLLOW US. Other sections unchanged.
- TOP CONTACT: main headline + Japanese lead shifted right so they clear the woman; UPDATE stack enlarged (~18–24px); inquiry pill larger (~252×70). Other TOP sections unchanged.
- `contact.html` KV: attached image (`assets/contact-kv.jpg`) used as-is as the page-top banner — no HTML overlay of the in-image type/button. White `.contact-kv` section, `object-fit: contain` + `object-position: center`. Form and company info unchanged.
- TOP NEWS (`index.html`): moved below ABOUT (order: … NUMBERS → ABOUT → NEWS → CONTACT). Restyled to mock layout — left column (large NEWS / 新着情報 / VIEW ALL →), vertical divider, right list with date+title rows and ← → arrows. Existing `newsItems` data unchanged. Card UI removed.
- TOP ABOUT US (`index.html`): new section `#about` between NUMBERS and CONTACT, faithful to the supplied mock. White full-width section (~1400px inner). Left: blue ABOUT US kicker + gray vertical rail, Noto Serif JP headline (伝わらないを、なくしていく。), body copy, pill CTA → `company.html`. Center-right: `assets/about-building.jpg` with diagonal `clip-path` polygon. Right: UPDATE/THE WAY… uppercase stack, large pale-blue Cormorant ABOUT, PEOPLE/TECHNOLOGY… stack, vertical 働くを、もっと好きに。 Desktop horizontal layout; tablet/mobile stacks copy → photo → deco. CONTACT and other sections unchanged.
- TOP NUMBERS (`index.html`): stats strip immediately under SERVICE, matched to the mock (5 equal columns, `#00539C` figures, 900 weight, smaller `+`). Copy: NUMBERS / 数字で見る / TSUTA-WORLD, 1.9億+ / 70社+ / 200万人+ / 400+.
- TOP SERVICE typography/layout (`index.html` only): matched the mock’s text placement without replacing banners. Larger blue 01–06 numbers; small light English labels sit beside numbers (never on photos). 01 is the featured split (copy / large photo / vertical 働くを、もっと好きに。) with “Fans Create the Future.” on the photo’s lower-center diagonal. 02–06 each use a different number/image/copy grid (not one repeated card). 16:9 `object-fit: contain` banners kept. Header / Hero / MEDIA unchanged.
- TOP SERVICE images: keep client 16:9 banners (`service-*.jpg`) uncropped. `object-fit: contain` + 16:9 frames; mock cover-crop removed so in-image type/logos stay visible. Layout (01 featured, 02–03 pair, 04–06 trio) unchanged. Header / Hero / MEDIA unchanged.
- TOP SERVICE (`index.html` only): rebuilt off the card grid into the mock composition. Content width ~1400px. Rows are featured 01 (copy + large diagonal photo + vertical 働くを、もっと好きに。), staggered 02–03 pair, 04–06 trio with overlapping numbers. Existing service copy, images, and links kept; photos cropped so numbers/copy lead. Header / Hero / MEDIA unchanged.
- TOP MEDIA type/image/padding scaled back up (layout unchanged) after the compact pass was too small.
- TOP MEDIA (`index.html` only): editorial 3-column row (MEDIA / compact 2 cards / copy + arrows). Existing `mediaItems` data, images, and YouTube links kept. Subpages unchanged.
- TOP Hero (index.html only) closer to the mock: pulled-back woman crop, bottom-right white diagonal cut (`clip-path` + `.hero-cut`), thinner left fade, `01` + long line pager, centered CTA arrow, smaller script, vertical `伝わるから、世界は動く。`, PEOPLE/BRANDS aside, left copy shifted up/left. Lower sections/subpages unchanged.
- TOP Hero right: "Create a New Standard." now uses `assets/hero-create-standard.png` matching the handwritten crop, placed top-right over the photo.
- TOP Hero left copy: UPDATE line is now horizontal black; AI lines use photo teal `#08608F`; subcopy is larger black; CTA shifted right toward the photo. Header/lower sections unchanged.
- TOP Header/Hero only (`index.html` + inline CSS): rebuilt to the new TSUTA-WORLD mock (white + pale blue, left copy, diagonal `assets/hero-woman.jpg`, script/vertical/pager decorations). Header is logo / SERVICE / MEDIA / NEWS / COMPANY / お問い合わせ / hamburger. Hero Lottie and GSAP entrance disabled (`scroll-motion.js` skips missing `#hero-lottie` and no-ops `initHero`). Lower sections and subpages unchanged.

## TOP Header / Hero file map (2026-09-09)
- TOP is `index.html` only (static HTML inside dc-runtime `<x-dc>`, not a React/Vue component). `site.css` is not used on TOP.
- Markup: `.top-hero-shell` wraps sticky `.top-header` (logo + `.top-header-nav`) and `#hero` (kicker / title / lead / `.hero-cta` + `.hero-visual` with photo / script / slogan / aside / pager). Lottie mount `#hero-lottie` is unused on TOP.
- CSS: almost all Header/Hero styles live in the inline `<style>` inside `<helmet>` in `index.html` (`.top-header*`, `.top-hero-*`, `.hero-*`, `#hero`).
- Behavior: `site-nav.js` injects phone hamburger for `.top-header-inner`; `scroll-motion.js` inits Wave Flow Lottie (`assets/hero-wave-flow.json`) and GSAP hero entrance. `atm-deco.js` skips `#hero`. `support.js` is dc-runtime (do not edit unless asked).

## Recent edits (2026-09-04)
- apparel.html hero: larger centered type (label / アパレル事業 / lead / icons / CTAs). Copy and links unchanged.
- Sitewide phone layout: hamburger nav (SERVICE / NEWS / COMPANY / CONTACT), tighter section padding, stacked CTAs, no horizontal overflow, 16px form fields. Copy and links unchanged.
- apparel.html hero: white centered banner with yellow marker on 自社ブランドのほか、, 4 service icons (brand / OEM / PR / EC), blue/white CTAs. Copy and links otherwise unchanged.
- animevideo.html hero title is two lines: 複雑な内容を、 / 誰にでも伝わる動画に。 Mark stays on 誰にでも伝わる. Right visual remains the overlapping work mosaic. Copy/links otherwise unchanged.
- Reverted Wave Flow Lottie on service heroes (fanstep / animevideo / creative-marketing / apparel). CONTACT blocks still use it. Copy and links unchanged.
- fanstep.html hero: white centered banner (yellow solid mark, pills, blue/white CTAs). Copy and links unchanged.
- Header nav (subpages `.page-header-nav` + TOP `.top-header-nav`): Space Grotesk, 700, 17px. CONTACT button scaled with it. Logo wordmark 19px, nowrap so it cannot crush.
- creative-marketing.html hero: left-aligned editorial split + overlapping work mosaic (Sammy offer / live-action / GREEN EYE). Copy and links unchanged.
- creative-marketing.html hero grouped: title break, catch tight under the heading, pills, body, then CTAs. Copy unchanged.
- Contact Wave Flow Lottie (`assets/hero-wave-flow.json`) applied to every Contact block, not just TOP: fanstep / animevideo / creative-marketing / apparel / website CTA sections, plus `contact.html` / `contact-thanks.html`. Shared CSS in `site.css`; init via `contact-lottie.js`. Copy and form endpoints unchanged.
- Reverted Speee-like boxed heading fill (`.hd-box`); yellow marker and plain titles restored.
- Service heroes (fanstep / animevideo / creative-marketing / apparel): removed right-side line-art diagrams; copy/CTAs/pills are centered. Copy and links unchanged.
- creative-marketing.html visual UI aligned with fanstep / animevideo (QUEUE / LLMO, light): yellow marker on 制作＆マーケティング, service pills, numbered service/area cards, pill works tabs, rounded work/contact cards, yellow result boxes. Copy, case studies, and links unchanged.
- fanstep.html PRODUCTION STYLE: dropped duplicate 制作スタイル label; body copy grouped into 3 numbered points (再生数を追わない / 意味のある1再生 / 安定志向の堅い制作) plus yellow quote and closing note.
- animevideo.html visual UI aligned with fanstep (QUEUE / LLMO, light): yellow marker, service pills, line-art diagram, 3-col service cards, 2-col works/voice, yellow testimonial, rounded contact card. Copy, portfolio data, and links unchanged.
- fanstep.html visual UI (QUEUE / LLMO, light): yellow keyword marker, issue pills, line-art diagram, restored 2×2 / 3-col cards, vs-compare for video assets, yellow quote/result boxes, contact form in a rounded card. Copy, works images, and tiktok_b form endpoint unchanged. `display-page` flatten overrides are scoped to `fs-page` only (`animevideo.html` untouched).
- TOP CONTACT: full-bleed Wave Flow Lottie (`assets/hero-wave-flow.json`) behind 「サービスに関するご相談は…」. `.top-contact-bg` is excluded from `.ui-atm` relative stacking so the animation sits absolutely behind the copy (not as a huge in-flow box). `contact-conversation.mp4` unused.
- TOP (`index.html`): Wave Flow Lottie init moved to after dc-runtime React mount (`waitForHeroLottie` in `scroll-motion.js`) — fixes invisible background when early init targeted removed `x-dc` DOM. Shell background `#f4f8fc`, lighter scrim/header glass.
- fanstep.html / animevideo.html: `display-page` layout — large Noto Sans JP 900 typography, generous section spacing (120–200px), single-column stacks per block, hero entrance + scroll reveal animations, decorative backgrounds and card chrome removed (flat white document). animevideo adds `av-reveal` scroll fade; fanstep re-enables `fs-hero-animate` / `fs-reveal`.
- fanstep.html / animevideo.html: removed hero stats (2億再生 / 92.5%・350社+・No.1).
- TOP hero/header background is the Lottie `Wave Flow.json` (`assets/hero-wave-flow.json`, lottie-web CDN), full-bleed behind copy. Previous `hero-business.mp4` unused and removed. Copy/links unchanged.
- Reverted the TOP hero video split layout.
- TOP scroll lock: `overflow-x: clip` on html/body was clipping vertical scroll; `support.js` also set html/body/`#dc-root` to `height: 100%`. Restored document scroll (`overflow-y: auto` on html, `height: auto`). Removed GSAP pin on TV/MEDIA grid so メディア掲載実績 no longer holds the page. Section deco clips on `.hero-deco` instead of `overflow: hidden` on whole sections.
- Geometric hero background is lighter sitewide (layer opacity ~0.38, thinner grid/wash/SVG).
- Sitewide spacing/type scale: more independent, startup-like whitespace. Section padding ~150–160px (hero ~180–200px). Heading-to-body 32–48px, heading-to-cards 48–72px, card gaps 40–48px, image-text ~48–72px, button gaps ~24px. PC side gutters `min(88px, 6.5vw)`, content width 1440px. Fonts, cards, and media enlarged so extra space does not look empty. TOP (`index.html`) and shared `site.css` (subpages/footer/forms).
- TOP hero/header implemented from the Figma-style mock. Brand color is a lighter standard blue `#3B82F6`. Buttons on brand fills use white text. Hero marker uses the same blue.
- TOP scroll motion via GSAP + ScrollTrigger (`scroll-motion.js`, CDN). Hero line reveal, media slide/pin (desktop), per-service clip-path, news row reveal, contact clip expand. Reduced-motion and mobile pin/parallax are limited. Copy/links unchanged.

## Client status (2026-09-02)
- Inquiry form implemented (`contact.html` → `contact-thanks.html`, `api/contact.js`).
- AI研修 footer copy is 全6コース.
- Company overview/history already matched https://tsuta-world.com/company/; no extra copy was supplied.
- Remaining: set `RESEND_API_KEY` (and a verified Resend from-domain if needed) on Vercel; website.html leftover vs min-nano.site; Koishikawa-sourced creative/apparel approval; NEWS thumbs; go-live domain/auth.

## Recent edits (2026-09-02)
- Contact mail is Resend-only (`RESEND_API_KEY`). nodemailer/SMTP removed. Thanks page only after successful send.
- Deploy path: push `main` to GitHub (`origin`) so the linked Vercel project rebuilds.

## What this is
Claude export of a static TSUTA-WORLD corporate site. Not a Node/npm app. Preview locally with `start-local.bat` or `py -m http.server 5500`.

## Main files
- `index.html` — TOP page (dc-runtime). Footer updated per client spec; NEWS links to post pages.
- `support.js`, `image-slot.js` — dc-runtime (do not edit unless asked).
- `middleware.js` — Vercel Routing Middleware (Basic auth for production)
- `contact.html` — お問い合わせフォーム（会社名 / お名前 / 役職 / 電話番号 / メール / 件名 / 内容）。送信後は `contact-thanks.html`。
- `contact-thanks.html` — 指定の自動返信文＋入力内容の確認。
- `contact-form.js` / `api/contact.js` — Resend のみ。`RESEND_API_KEY` で管理者通知（info@tsuta-world.com）と自動返信の2通。キー未設定・送信失敗時はエラー表示、成功時のみサンクスへ。
- `package.json` — Vercel deploy (`@vercel/functions`)
- `start-local.bat` — double-click to start local preview (Python http.server on port 5500)
- `site.css` — shared styles for subpages.
- `post.html` — 新着情報一覧
- `post-*.html` — article detail pages (content from tsuta-world.com)
- `company.html` — 会社情報（代表メッセージ / 会社概要 / 沿革）。公式 https://tsuta-world.com/company/ と同一内容。本社は渋谷区神泉町 アネックス神泉301。
- `agency.html` — 代理店募集 (from existing site partner-recruitment text)
- `creative-marketing.html` — クリエイティブ＆マーケティング詳細（Hero / SERVICES / WORKS / CONTACT / Footer）
- `fanstep.html` — ファンステップ採用 LP。Word「fanstep_service_description.docx」準拠（課題4つ / 実績スライド5枚 / 制作スタイル / 3施策 / 動画資産 / 3成果 / tiktok_b問い合わせフォーム / フッター）。料金・ISZ事例・タイムラインは含めない。TOPカードからリンク済み。
- `assets/fanstep/` — Word内の実績スライドPNG（image1/3/5/7/9）と付随SVG。
- `website.html` — Webサイト制作・リース 詳細（同構成、placeholderコンテンツ）
- `animevideo.html` — 説明用アニメ動画 TSUTA-WORLD 詳細。Hero（service-anime.jpg）/ SERVICES 3種 / WORKS 全10カテゴリ75作品（公式LPから取得、assets/animevideo/ サムネ）/ VOICE / CONTACT。カテゴリフィルタはJS。
- `apparel.html` — アパレル 詳細。Hero / 事業について / 自社ブランド・OEM / 対応範囲 / WORKS / CONTACT / Footer。コイシカワapparel掲載の文言と実績のみ。`ap-page` で大きな太字（Noto Sans JP 900 / Barlow Condensed）とスクロール表示アニメ。
- `assets/apparel/` — BYWEAR・MA LABELバナー、AKG実績画像（コイシカワ公式から取得）
- `assets/ai-dx-training.png` — AI/DX研修（サービス03）サムネイル
- `assets/service-*-photo.jpg` — TOP SERVICE 01–06 photo banners from the 9/5 mock. Older graphic `service-*.jpg` files remain unused on TOP.
- `assets/media-eejanai.png` — TV/MEDIA カンニング竹山のイチバン研究所 thumbnail
- `assets/media-ichiban.png` — TV/MEDIA ええじゃない課Biz thumbnail
- `assets/about-building.jpg` — TOP ABOUT US section building photo (diagonal clip-path)

## Recent edits (2026-09-02)
- Contact form + thanks page per client spec. Admin mail to info@tsuta-world.com, subject お問い合わせがありました. Autoreply/thanks copy as specified, with submitted fields.
- Footer AI研修表記を全6コースに変更（全ページ）。
- 会社概要・沿革は公式 company ページと一致済みのため文言変更なし。

## Recent edits (2026-08-29)
- TOP TV/MEDIA: larger cards, full-bleed thumbs, play overlay, Noto Sans JP for Japanese copy, bigger titles/body, 「動画を見る」CTA.
- TOP NEWS / SERVICES: same 1360px width as TV/MEDIA; Noto Sans JP; larger titles; NEWS rows are full-width clickable; service cards full-bleed with 「詳しく見る」.

- Phase 4C: 企業情報系（company / agency / post / post-* / contact）のUIのみ調整。文言・会社情報・記事は未変更。TOP・サービス詳細は未変更。
- Phase 4B: サービス詳細5ページのUIブラッシュアップ。文言・実績・リンクは未変更。
- Phase 4A: TOP（`index.html` のみ）のUIブラッシュアップ。文言・サービス・NEWS記事は未変更。ヒーロー／ヘッダーは Phase 4A 前のテキストヒーロー＋すりガラスヘッダーに戻し、テキストは中央揃え。TV/MEDIA・NEWS・SERVICES・CONTACT・Footer のブラッシュアップは維持。他詳細ページは未変更。
- `apparel.html` を見やすく拡大。見出し・本文を太字化、幅1360px、ヒーローKen Burns、カード／実績のスクロールフェード。文言・実績は据え置き。他ページの共通 `cm-*` は変更なし。

## Recent edits (2026-08-31)
- Phase 3B: `apparel.html` をコンテンツ拡充。コイシカワapparelの事業文言、自社ブランド（BYWEAR / MA LABEL BY MAISON SPECIAL）、OEM/ODM対応、確認できる実績2件（HARD ROCK COLLECTION 2024、BYWEAR×AKG 2022）を掲載。未確認の数字・企業名は未記載。TOP / creative-marketing 他ページは未変更。
- Phase 3A: `creative-marketing.html` をコンテンツ拡充。コイシカワ公式の制作＆マーケティング文言／4ツール分類／対応領域、既存SNS・実写実績、コイシカワ掲載の代表6案件を大きなCASEで追加。未確認の数字・企業概要は未記載。TOP他ページは未変更。
- TOP NEWS: ZIP「NEWSセクションUI改善案」の案Aを適用。既存リスト構造のまま幾何グリッド／円／グラデーション背景を挿入。記事3件・リンクは据え置き。案B/Cは記事画像が必要なため未採用。
- サイト全体の青UI統一: NEWSの薄青グラデーション＋グリッド／円をヒーロー・ヘッダー・TV/MEDIA・SERVICES・CONTACT・フッター、およびサブページのHero/Header/WORKS/CONTACT/page-mainへ展開。CONTACTボタンは `#0F7490`。
- TOP hero: コラージュ画像を外し、白背景のテキストヒーローに戻した。
- Footer を3カラムに再編（全ページ共通）: 左＝サービス一覧＋AI研修、中央＝COMPANY（新着情報を統合、POST見出し削除）、右＝ロゴ＋既存会社概要。本文は未変更。
- TOP `index.html` クライアント修正: 「オリジナル/関連サービス」見出し削除、サービス01〜06連番化、薄青背景を全6件に統一、リード文・サービス名・アパレル説明・CONTACT 2行表示を更新。

## Recent edits (2026-08-26)
- TOP hero: ウェーブ背景画像を外し、白背景のテキストヒーローに戻した。
- Vercel 404/読み込み失敗対策: `middleware.js` が静的ファイル（support.js / assets）を Basic 認証対象にしていた問題を修正。認証 env 未設定時は通過。`vercel.json` で `media-ichiban.html` → YouTube、`favicon.ico` → アイコンPNG へリダイレクト。`index.html` に favicon リンク追加。
- `animevideo.html` — 説明用アニメ動画ページ実装。公式LP（tsuta-world.com/lp/）から75作品・YouTube ID・サムネイルを取得し `assets/animevideo/` に配置。10カテゴリフィルタ、実績グリッド、クライアントの声、LP/YouTubeリンク。`site.css` に av-* スタイル追加。
- `creative-marketing.html` — PPTX「動画制作に関する実績掲載」準拠で WORKS 実装。`assets/creative-marketing/` にPPTから切り出し（実写6/SNS6/アニメLP+口コミ）。カテゴリタブ（実写・SNS・アニメ）、YouTube/LP/tiktok_b リンク付き。
- TOP「Webサイト制作・リース」カード → `https://min-nano.site/`（新規タブ）。`website.html` は未リンクのまま残置。
- `fanstep.html` hero copy restored to Word: 「採用ブランディング動画 SNS」and the one-line four issues including「モチベーション低下」. Inquiry form still posts to `https://tsuta-world.com/tiktok_b/branch.php`.
- `fanstep.html` rebuilt from `fanstep_service_description.docx`: shorter production-style copy, 実績 gallery (`assets/fanstep/image1.png` etc.), three measures, video-asset contrast, three outcomes + RESULT. Dropped PDF extras (pricing, ISZ, timeline, company profile).
- Previous pass: `fanstep.html` from `ファンステップ採用.pdf` (19 pages). Company address matches current HQ (神泉), not older 恵比寿 listings.

## Recent edits (2026-08-25)
- Mobile/tablet hardening: grids use `minmax(0, 1fr)` so columns cannot overflow; TOP/media/service stack at 960px; footer 5→2→1; hero tags scale down (30→26→22→16px); header extra links hide at 960px. Overflow-checked at 320/375/414/768/1024/1280 (scrollWidth === clientWidth).
- `media-ichiban.html`: video is now a full-width click-to-play player (YouTube iframe loads only after tap) with stills stacked below; removed side-by-side layout that made the player too small.
- TOP SERVICES switched from 108px square thumbs to large 16:9 cards so promotional banners are fully visible. Images mapped to existing services: ファンステップ採用, キタバズ, Webサイト制作・リース, 説明用アニメ動画, クリエイティブ＆マーケティング, アパレル.
- Added 4 missing service detail pages (TOP SERVICES cards were `href="#"`): `fanstep.html`, `website.html`, `animevideo.html`, `apparel.html`. Same layout as `creative-marketing.html` (cm-* classes in site.css, reused as-is). TOP `index.html` originalServices/synergyServices hrefs updated to point to the new pages. Body/case content uses "後から追加できます" placeholders like creative-marketing.html did.

## Recent edits (2026-08-24)
- Fixed `media-ichiban.html` mojibake + broken footer HTML; responsive layout in `site.css` and `index.html`.
- Rewrote all five `post-*.html` detail pages with correct UTF-8 Japanese (fixed mojibake and broken HTML tags). Each uses `page-header-inner--nav` header, breadcrumb/post-meta layout, and footer identical to `contact.html`.
- Added `creative-marketing.html` layout page; TOP synergy link → creative-marketing.html.
- Vercel Routing Middleware (`middleware.js`) protects entire site with Basic auth via `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD` env vars. Minimal `package.json` only for `@vercel/functions` (Vercel deploy; local preview still uses `start-local.bat` without auth).
- Hero tags enlarged to 30px (PC) / 26px (mobile); sub-catch remains 18px.

## Recent edits (2026-08-21)
- Hero background video removed; hero is text-only on white background.

## Recent edits (2026-08-20)
- Hero video: white background + scale(1.36) cover crops top/bottom black letterbox.
- Footer and body copy darkened (#1a1a1d / #2e3136); footer links font-weight 600.

## Recent edits (2026-08-20)
- Hero right `image-slot` replaced with looping muted `assets/hero.mp4`; overflow hidden + scale(1.42) crops top/bottom black bars.

## Footer (TOP + all subpages)
- 4 columns on desktop: brand / SERVICE / COMPANY / FOLLOW US. Dark navy `#001722`, white type, vertical rules after SERVICE and COMPANY.
- SERVICE: ファンステップ採用, キタバズ, AI × Web制作, 説明用アニメ動画, クリエイティブ＆マーケティング, アパレル
- COMPANY: 会社概要, メンバー (URL unset), 採用情報, お知らせ, お問い合わせ
- FOLLOW US: Instagram / YouTube / TikTok / X; privacy + sitemap; © 2026 TSUTA-WORLD Inc.

## Git
Local repo initialized. Remote `origin` = https://github.com/hugosoderberg11/tsuta-world.git (empty, no commits yet). GitHub account connected in Cursor.
`py -m http.server 5500`
- TOP: http://localhost:5500/ (index.html)
- Posts: http://localhost:5500/post.html

## Recent edits (2026-08-20)
- Client spec: footer restructure, subpages, news list/detail, AI training links.
- Hero h1 first line kept on one line via white-space: nowrap.
